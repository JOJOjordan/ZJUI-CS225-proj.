//
// Created by jordan on 2021/4/17.
//
#include <unordered_set>
#include <set>

#include "graph.h"
using namespace std;


// Here we are going to implement a Hungary algorithm. the algorithm will travel between unpaired path and
// paired path to expand the partial paired group.
// we are going to make green hats to the previous boys lol.

 template<class T> int boys2girls(vertexlist<T> &vlist)
 {
 }

 template<class T> int girls2boys(vertexlist<T> &vlist)
 {
 }

 template<class T> int unordered(vertexlist<T> &paired)
 {}

 template<class T> int reachable(vertex<T>* boy,edgelist<T>* path, vertex<T>* girl)
 {

 }

 template<class T> int pathmaker(vertex<T>* boys, graph<T>* direct, vertex<T>* girls)
 {
     set<pair<T,T>> aug_path;
     elist<T> neighbour = boys->getedges();


 }

 template<class T> int make_perfect_pair(edgelist<T>* edge)
 {
     // here we can implement the main program.
     // mainly implement according to the fake codes.
 }